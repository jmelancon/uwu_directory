<header class="fixed-top shadow">
    <nav class="navbar navbar-expand bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path("root") }}">
                <img height="32px" class="pe-1" src="{{ asset("img/logo.svg") }}" alt="Site Logo"/>
                uwu_directory
            </a>
            <div class="navbar-nav">
                <a class="nav-link" href="https://discord.gg/HtpK4rV8R2">Discord</a>
                <a class="nav-link" href="https://acm.cs.und.edu/wiki">Wiki</a>
                {% if is_granted("ROLE_SSO_ADMINISTRATORS") %}
                    <a class="nav-link" href="{{ path("administratorIndex") }}">Admin</a>
                {% endif %}
                {% if is_granted("ROLE_USER") %}
                    <a class="nav-link" href="{{ path("logout") }}">Log out</a>
                {% else %}
                    <a class="nav-link" href="{{ path("loginForm") }}">Log in</a>
                {% endif %}
            </div>
        </div>
    </nav>
</header>